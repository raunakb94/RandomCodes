#include<bits/stdc++.h>
using namespace std;
int diff(int a,int b)
{
    return max(a-b,b-a);
    max()
}
int main()
{
    int T;
    cin>>T;
    while(T--)
    {
        int N;
        cin>>N;
        vector<int>arr;
        while(N--)
        {
            int d;
            cin>>d;
            arr.push_back(d);
        }
        int dp[arr.size()][2];
        memset(dp,0,sizeof(dp));
        dp[0][0]=0;
        dp[0][1]=arr[0];
        dp[1][0]
    }

}
